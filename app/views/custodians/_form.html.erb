<%= form_with(model: custodian) do |form| %>
  <% if custodian.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(custodian.errors.count, "error") %> prohibited this custodian from being saved:</h2>

      <ul>
        <% custodian.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
